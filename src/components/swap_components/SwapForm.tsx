import React from 'react';

const SwapForm = ({ fromToken, toToken, amount, onSwap }) => {
  return (
    <div className="form-block">
      {/* Token inputs and swap button go here */}
    </div>
  );
};

export default SwapForm;
